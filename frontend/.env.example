# When unset, the bundle uses same-origin /api/v1 (recommended for production
# behind a reverse proxy / nginx).
#
# In local dev, the Vite dev server proxies /api/v1 to the URL below, so you
# typically only need to set this if your backend is on a non-default host.
VITE_API_URL=http://localhost:8000
